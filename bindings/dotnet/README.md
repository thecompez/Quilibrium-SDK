# .NET binding

`Quilibrium.cs` is a zero-package P/Invoke wrapper over the stable C ABI. Ensure `quilibrium` (`libquilibrium.so`, `libquilibrium.dylib`, or `quilibrium.dll`) is discoverable by the runtime.
